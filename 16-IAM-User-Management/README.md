

# IAM User Management — Project Summary

This small project demonstrates a Terraform-based workflow to provision IAM users from a CSV file and apply simple configurations and tags. It's intended as a hands-on example for learning Terraform data sources, csvdecode usage, and basic IAM resources.

